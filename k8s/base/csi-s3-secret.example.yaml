apiVersion: v1
kind: Secret
metadata:
  name: csi-s3-secret
  namespace: messager
type: Opaque

stringData:
  accessKeyID: "minioadmin"
  secretAccessKey: "minioadmin"

---

apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: messager
type: Opaque

stringData:
  accessKeyID: "minioadmin"
  secretAccessKey: "minioadmin"
